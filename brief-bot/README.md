# clatz-brief-bot — Telegram-бот брифа для клиентов

Бот, которому менеджер после созвона отправляет клиента для заполнения брифа.
Работает как Cloudflare Worker (webhook), состояние хранится в KV — клиент
может закрыть чат и вернуться позже, все ответы сохраняются.

## Что умеет

- **Согласие** — перед стартом фиксирует согласие на обработку ПДн
  и условия работы (кнопка «Принимаю», сохраняется дата и время).
- **Пошаговый бриф** (25 шагов): контакты (имя, телефон с кнопкой
  «поделиться контактом», e-mail, компания) → ниша → ссылки по каждой
  площадке отдельным вопросом (сайт, ВКонтакте, Telegram, YouTube,
  Дзен, Instagram*) с кнопкой «Не использую» → опыт в нише и опыт
  продвижения → конкуренты (можно пропустить) → кто ведёт контент →
  видео-отзывы и контент-план (5 вариантов-кнопок) → 8 рекламных
  каналов с бюджетами за последние 3 месяца → вопрос о публикации
  записи исследования в соцсетях агентства.
- **Возврат к шагам** — команда `/edit` или кнопка «Изменить ответы»:
  клиент выбирает любой заполненный шаг, меняет ответ и возвращается
  туда, где остановился. При повторном `/start` бот предлагает
  продолжить с места остановки.
- **Напоминания** — если бриф не закончен: первое напоминание после
  2 часов тишины, второе после 24 часов. Шлются только в рабочее время
  (10:00–20:30 МСК; константы в начале `worker.js`), таймер
  сбрасывается при любой активности клиента.
- **Доставка** — в Telegram-чат менеджера (`ADMIN_CHAT_ID`) приходит
  короткое уведомление «Бриф заполнен от пользователя @username, Имя»,
  а сами данные уходят письмом на почту (`EMAIL_TO`): Excel-файл (.xlsx)
  с брифом + логотипы во вложении. Если письмо отправить не удалось,
  бриф целиком дублируется в Telegram, чтобы данные не потерялись.
  Перед отправкой клиент видит сводку и подтверждает её.

## Развёртывание

### 1. Создай бота

В Telegram у [@BotFather](https://t.me/BotFather): `/newbot` → имя, например
`К.Л.А.Ц | Бриф` и username вида `clatz_brief_bot`. Сохрани **токен**.

Там же желательно настроить команды: `/setcommands` →

```
start - Начать или продолжить бриф
edit - Изменить заполненные ответы
help - Что умеет бот
```

### 2. Узнай ADMIN_CHAT_ID

Куда слать готовые брифы. Проще всего: напиши новому боту `/start` со своего
аккаунта (или добавь его в рабочую группу), затем открой
`https://api.telegram.org/bot<ТОКЕН>/getUpdates` — в ответе будет
`"chat":{"id": ...}`. Это и есть id (у групп он отрицательный).

### 3. Включи почту (Email Routing)

Брифы уходят письмом через нативную почту Cloudflare — бесплатно,
без сторонних сервисов. В дашборде Cloudflare:

1. Зона `clatz.ru` → **Email → Email Routing** → включи (Cloudflare сам
   добавит нужные MX/SPF-записи).
2. **Destination addresses** → добавь почту, куда слать брифы
   (например, рабочий Gmail) → подтверди по письму. Это будет `EMAIL_TO`.
3. Отправитель (`EMAIL_FROM`) — любой адрес на домене зоны,
   например `brief@clatz.ru` (создавать ящик не нужно).

### 4. Создай KV и задеплой

```bash
cd brief-bot
npm install
npx wrangler kv namespace create BRIEF_KV
# → выведет id — впиши его в wrangler.jsonc вместо ЗАМЕНИ_НА_ID_ИЗ_WRANGLER

npx wrangler secret put BOT_TOKEN       # токен от BotFather
npx wrangler secret put ADMIN_CHAT_ID   # id чата менеджера
npx wrangler secret put WEBHOOK_SECRET  # любая случайная строка, придумай сам
npx wrangler secret put EMAIL_FROM      # напр. brief@clatz.ru
npx wrangler secret put EMAIL_TO        # подтверждённый адрес из шага 3

npx wrangler deploy
# → выведет URL вида https://clatz-brief-bot.<аккаунт>.workers.dev
```

### 5. Привяжи webhook

Подставь свой токен, URL воркера и тот же WEBHOOK_SECRET:

```bash
curl "https://api.telegram.org/bot<ТОКЕН>/setWebhook" \
  -d "url=https://clatz-brief-bot.<аккаунт>.workers.dev/webhook" \
  -d "secret_token=<WEBHOOK_SECRET>" \
  -d "drop_pending_updates=true"
```

Проверка: `https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo` — поле `url`
должно совпадать, `last_error_message` — отсутствовать.

### 6. Проверь сценарий

1. Напиши боту `/start`, пройди пару шагов, закрой чат, снова `/start` —
   бот должен предложить продолжить с того же места.
2. `/edit` — изменение любого пройденного шага и возврат.
3. Дозаполни до конца — в `ADMIN_CHAT_ID` придёт уведомление
   «Бриф заполнен от пользователя …», а на `EMAIL_TO` — письмо
   с Excel-файлом брифа.
4. Напоминания можно не ждать 2/24 часа: cron видно в дашборде Cloudflare
   (Workers → clatz-brief-bot → Triggers), логи — `npx wrangler tail`.

## Как выглядит поток для клиента

```
/start → согласие ПДн → имя → телефон → e-mail → компания → ниша →
сайт → ВК → Telegram → YouTube → Дзен → Instagram* → опыт в нише →
опыт продвижения → конкуренты → кто ведёт контент →
видео-отзывы и контент-план →
8 каналов: таргет ВК, Директ, TG Ads, блогеры, SEO, Авито,
офлайн, другие (бюджет/мес за последние 3 месяца или «Не использовали») →
публикация исследования (да / обсудим / нет) →
сводка → «Отправить бриф» → 🎉
→ менеджеру в TG: «Бриф заполнен от пользователя @username, Имя»
→ на почту: письмо с Excel-файлом брифа
```

## Файлы

- `worker.js` — весь код бота (webhook + cron + почта), без зависимостей
- `wrangler.jsonc` — конфиг воркера: KV-биндинг `BRIEF_KV`,
  email-биндинг `BRIEF_EMAIL`, cron `*/30 * * * *`
- Ссылка на политику конфиденциальности в согласии: `https://clatz.ru/privacy.html`
  (константа `PRIVACY_URL` в начале `worker.js`)
